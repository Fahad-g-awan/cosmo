another thing aboiut manager is that if manager is linked with parent i mean has access to that parent clinic mean like the parent clinic is added in its clinic list by anyother main manager then tht manager also becomes like a main manager and can then see all branches and parent clinic as well but otherwise its if any 1 or more rbanches are selected then it will remain confined to those only
i know this rule is more like related to permissions not relations but u see how it can be added now if it is linked with relations work but do remember and keep in ur memory when are working for permissions
