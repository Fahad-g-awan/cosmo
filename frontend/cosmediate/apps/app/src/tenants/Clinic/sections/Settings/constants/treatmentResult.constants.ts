export const ALLOWED_FIELDS = new Set([
  "clinicTreatmentId",
  "description",
  "beforeImage",
  "afterImage",
]);

export const UPDATE_ALLOWED_FIELDS = new Set([
  "clinicTreatmentId",
  "description",
  "beforeImage",
  "afterImage",
]);
