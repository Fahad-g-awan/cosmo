export const ALLOWED_FIELDS = new Set(["name", "published"]);

/** Min length for a treatment brand name (create + update). */
export const BRAND_NAME_MIN_LENGTH = 2;

/** Max length aligned with backend BrandName. */
export const BRAND_NAME_MAX_LENGTH = 500;
