export const ALLOWED_FIELDS = new Set(["name", "published"]);

/** Min length for a treatment category name (create + update). */
export const CATEGORY_NAME_MIN_LENGTH = 2;

/** Max length aligned with backend CategoryName. */
export const CATEGORY_NAME_MAX_LENGTH = 500;
